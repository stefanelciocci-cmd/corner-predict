app = "football-prediction-bot"
primary_region = "fra"

[build]

[env]
  DATABASE_PATH = "/data/football_bot.db"

[mounts]
  source = "football_bot_data"
  destination = "/data"

[processes]
  app = "python -m bot.main"
